version: '3'
services:
  nodejs:
    container_name: restful-nodejs
    command: ["npm", "start"]
    image: node:8.10.0
    working_dir: /usr/src/app
    restart: always
    tty: true
    ports:
      - "8001:8001"
    volumes:
      - ./project:/usr/src/app
    networks:
      - restful-net
    links:
      - mongodb
  mongodb:
    container_name: restful-mongodb
    image: mongo:3.7
    restart: always
    tty: true
    ports:
      - "27017:27017"
    volumes:
      - restful-data:/data/db
  mongo-express:
    container_name: restful-mongo-express
    image: mongo-express
    ports:
      - "8081:8081"
    restart: always
    tty: true
    environment:
      - "ME_CONFIG_MONGODB_SERVER=mongodb"
      - "ME_CONFIG_BASICAUTH_USERNAME="
      - "ME_CONFIG_BASICAUTH_PASSWORD="
  swagger-ui:
    container_name: restful-swagger-ui
    image: swaggerapi/swagger-ui
    restart: always
    tty: true
    ports:
      - "80:8080"
    environment:
      - "API_URL=http://localhost:8001/swagger"
networks:
  restful-net:
volumes:
  restful-data:
